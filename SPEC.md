# DQW ダメージ計算アプリ 仕様書

## アプリの目的
ドラクエウォークの物理ダメージを計算し、3パターンを並べて比較できるツール。

## 計算式

### 基礎ダメージ
```
基礎ダメージ = floor(攻撃力 / 2) - floor(守備力 / 4)
最小値: 1
```

### 最終ダメージ
```
最終ダメージ = floor(
  基礎ダメージ
  × (スキル倍率 / 100)
  × ((100 + Σ①) / 100)   ← 斬撃&体技ダメージアップ
  × ((100 + Σ②) / 100)   ← 属性ダメージアップ
  × ((100 + Σ③) / 100)   ← 系統特効
  × (④弱点 / 100)        ← 弱点&耐性（敵側）
  × ((100 + Σ⑤) / 100)   ← ボス特効
  × ((100 + Σ⑥) / 100)   ← 特殊補正
)
```

同カテゴリ内の補正は加算、異カテゴリ間は乗算。

### 会心・超会心
- 会心ダメージ   = floor(最終ダメージ × 1.25)
- 超会心ダメージ = floor(最終ダメージ × 1.50)

## 入力形式

| 項目 | 入力例 | 意味 |
|------|--------|------|
| スキル倍率 | 230 | 230% → ×2.30 |
| ①②③⑤⑥各ボーナス | 20 | +20% → (100+20)/100 = ×1.20 |
| ④弱点（敵側） | 150 | 150% → ×1.50（弱点=150, 等倍=100, 耐性=50など） |

## 参考例（サイトのギガスラ計算）
```
基礎194 × 230% × 139% × 140% × 100% × 150% × 100% × 100% = 1,302
```

## 画面構成

### 3行の入力カード
- 行1 / 行2 / 行3 の3パターンを同時入力・比較可能
- PC幅（900px以上）: 3列横並び
- スマホ（900px未満）: 縦スクロール

### 各行のカード内容
- 攻撃力 / 守備力 / スキル倍率
- 会心種別: [なし] [会心×1.25] [超会心×1.50]
- ①〜⑥ 補正カテゴリ（①②③⑤⑥は複数入力可、④は単一）
- 計算結果: 基礎ダメージ / 最終ダメージ / 会心 / 超会心

### コピーボタン
- 行1: 右（行2）へコピー
- 行2: 左（行1）からコピー / 右（行3）へコピー
- 行3: 左（行2）からコピー

## やらないこと
- スキルのプリセット選択（倍率は直接入力）
- データ保存・履歴
- 呪文ダメージ計算（物理のみ）
